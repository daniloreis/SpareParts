@model Domain.MaterialAtendimentoTransferencia

@using (Html.BeginForm("Save", "Materialtransferencia"))
{
    @Html.AntiForgeryToken()
    <tr>
        <td>
            @Html.EditorFor(model => model.CodMaterial, new { htmlAttributes = new { @class = "form-control", type="number" } })
        </td>
        <td>
            <input type="submit" value="Incluir" class="btn btn-primary" />
        </td>

    </tr>
}

